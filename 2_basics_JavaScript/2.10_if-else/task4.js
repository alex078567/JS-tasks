/**
 * let result;
 * if (a + b < 4) {
 * result = 'Мало';
 * } else {
 * result = 'Много';
}
 */

// переписать с использованием условного оператора '?'

'use strict';

const result = a + b < 4 ? 'Мало' : 'Много';
